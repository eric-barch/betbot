import * as db from '../../db';
import * as pageParsers from '../../pageParsers';

export async function init() {
    // const gamePages = await db.ExchangeLeaguePage.findAll({
    //     where: {
    //         pageName: 'games',
    //     }
    // });

    // const parsers = new Array<pageParsers.PageParser>;

    // for (const gamePage of gamePages) {
    //     const parser = await gamePage.getParser();
    //     parsers.push(parser);
    // }
}